apiVersion: v1
kind: Service
metadata:
  name: {{ include "walkthrough.fullname" . }}
  labels: {{- include "walkthrough.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{- include "walkthrough.selectorLabels" . | nindent 4 }}
